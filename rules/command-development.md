---
name: command-development
description: Rules for writing Claude Code slash commands
paths: ["**/commands/**/*.md", ".claude/commands/**", "~/.claude/commands/**"]
---

When editing slash command files, use the `commands-authoring` skill for full authoring guidance on frontmatter, arguments, dynamic content, invocation control, and anti-patterns.
